{
  "name": "api-services",
  "version": "0.0.1",
  "description": "API as a Services",
  "repository": "git@github.com:hieudoanm/apis-services.git",
  "author": "hieudoanm <hieumdoan@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build:elo": "yarn workspace elo build",
    "build:proxy": "yarn workspace proxy build",
    "build:tarot": "yarn workspace tarot build",
    "build": "yarn build:elo && yarn build:proxy && yarn build:tarot",
    "dev:elo": "yarn workspace elo dev",
    "dev:proxy": "yarn workspace proxy dev",
    "dev:licenses": "yarn workspace licenses dev",
    "dev:tarot": "yarn workspace tarot dev",
    "dev:visas": "yarn workspace visas dev",
    "test:elo": "yarn workspace elo test",
    "test": "yarn test:elo",
    "lint": "eslint . --fix --ext .ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.27.1",
    "@typescript-eslint/parser": "^5.27.1",
    "eslint": "^8.17.0",
    "husky": "^8.0.1",
    "prettier": "^2.6.2",
    "pretty-quick": "^3.1.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
